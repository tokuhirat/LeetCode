# 387. First Unique Character in a String
## STEP1
- 何も見ずに解いてみる
```python
from collections import defaultdict


class Solution:
    def firstUniqChar(self, s: str) -> int:
        letter_to_count = defaultdict(int)
        for letter in s:
            letter_to_count[letter] += 1
        for index, letter in enumerate(s):
            if letter_to_count[letter] == 1:
                return index
        return -1
```
#### memo
- まず文字の出現回数を数えて、その後先頭から見ていき出現回数が１回の文字があれば出力。
- lowercase English letter でない入力がきてもそれを含めて重複の確認をする方針にした。

n = len(s) として  
時間計算量: $O(n)$  
空間計算量: $O(1)$  

## STEP2
### プルリクやドキュメントを参照
- https://github.com/olsen-blue/Arai60/pull/15/files
    - 長さ 26 の配列で管理する場合は入力のチェックが必要。
    - Counter の実装について確認
        - <https://github.com/python/cpython/blob/main/Lib/collections/__init__.py#L551>
        - <https://docs.python.org/3/reference/datamodel.html#object.__missing__>  
        KeyError を出さないためのメソッド
        - ついでに defaultdict も確認しようと思ったが、読み解けなくて諦めた。  
        https://github.com/python/cpython/blob/3.13/Modules/_collectionsmodule.c#L2175
    - 連想される事として、
        - 3.7から dict は挿入順が保存されるようになった。
          https://docs.python.org/3/library/stdtypes.html#dict  
        - linked list + hashmap で書ける。時間がある時に実装してみる。  
          https://github.com/tarinaihitori/leetcode/pull/15/files#r1831290136
- https://github.com/hroc135/leetcode/pull/15/files
    > サロゲートペア・結合文字列・合字
    - 文字に関して、知らない知識だった。ひとまずこのような複雑な文字種の存在を知っていて必要に応じて調べられる、という感覚になるようにしたい。
    

Counter を使うバージョン
```python
from collections import Counter


class Solution:
    def firstUniqChar(self, s: str) -> int:
        letter_to_count = Counter(s)
        for index, letter in enumerate(s):
            if letter_to_count[letter] == 1:
                return index
        return -1
```

## STEP3
### 3回ミスなく書く
Counter はこれまで書いていなかったので練習する
```python
from collections import Counter


class Solution:
    def firstUniqChar(self, s: str) -> int:
        letter_to_count = Counter(s)
        for index, letter in enumerate(s):
            if letter_to_count[letter] == 1:
                return index
        return -1
```

1分,1分,1分で3回Accept
